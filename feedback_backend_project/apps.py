from django.apps import AppConfig

class FeedbackBackendProjectConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'feedback_backend_project'  # Ensure this matches the app folder name
